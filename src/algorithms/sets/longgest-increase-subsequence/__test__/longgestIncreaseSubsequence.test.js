import longgestIncreaseSubsequence from '../longgestIncreaseSubsequence';

describe('longgestIncreaseSubsequence', () => {
  it('should calculate longgest increase subsequence', () => {
    expect(longgestIncreaseSubsequence()).toBeUndefined();
  });
});
