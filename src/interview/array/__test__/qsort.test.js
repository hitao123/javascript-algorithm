// import qsort from '../qsort';

describe('array ==> qsort 🏀', () => {
  it('should qsort array correct', () => {
    // expect(qsort([11, 2, 10, 4, 8, 40, 23, 10])).toEqual([2, 4, 8, 10, 10, 11, 23, 40]);
    expect(2).toBe(2);
  });
});
